# Extract Text Node

## Type

`extract_text`

## Purpose

Extracts text from an element and saves it as a variable.

## Config

```json
{
  "selector": { "required": true, "type": "string" },
  "save_as": { "required": true, "type": "string" }
}
```

Allowed keys: `selector`, `save_as`.
