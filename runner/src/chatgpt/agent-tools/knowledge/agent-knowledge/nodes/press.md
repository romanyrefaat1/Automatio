# Press Node

## Type

`press`

## Purpose

Presses a keyboard key on an element.

## Config

```json
{
  "selector": { "required": true, "type": "string" },
  "key": { "required": true, "type": "string" }
}
```

Allowed keys: `selector`, `key`.
