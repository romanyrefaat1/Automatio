# Select Node

## Type

`select`

## Purpose

Selects an option in a select element.

## Config

```json
{
  "selector": { "required": true, "type": "string" },
  "value": { "required": true, "type": "string" }
}
```

Allowed keys: `selector`, `value`.
