# Parallel Node

## Type

`parallel`

## Purpose

Runs eligible branches of the workflow in parallel according to runtime semantics.

## Config

```json
{
  "merge_variables": {
    "required": false,
    "type": "boolean"
  }
}
```

Allowed key: `merge_variables`.
