import {NodeTypes} from "@/types/types"

export default function SubTabContent(type: NodeTypes) {

    return (
        <div>Tabs Content goes here <br />
            <button>Add Node</button>
            </div>
    )
}